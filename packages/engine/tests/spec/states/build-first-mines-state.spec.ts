import { BuildFirstMinesState } from '../../../src/states/build-first-mines-state';

describe('Build First Mines State', () => {
  let state: BuildFirstMinesState;

  it('will determine next player correctly on first pass', () => {});
});
